const authRoutes = require('./auth-routes');
const profileRoutes = require('./profile-routes');
const registrationRoutes = require('./register-routes');

module.exports = {
    authRoutes,
    profileRoutes,
    registrationRoutes,
}